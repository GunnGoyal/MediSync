<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Doctor Login</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('https://images.pexels.com/photos/4197564/pexels-photo-4197564.jpeg') no-repeat center center fixed;
            background-size: cover;
            min-height: 100vh;
            margin: 0;
        }
        .container {
            max-width: 400px;
            margin: 80px auto;
            background: rgba(240,248,255,0.95);
            padding: 32px;
            border-radius: 12px;
            box-shadow: 0 2px 16px rgba(0,0,0,0.12);
            text-align: center;
        }
        h2 {
            color: #007bff;
            margin-bottom: 32px;
        }
        label {
            display: block;
            margin-top: 16px;
            color: #333;
        }
        input {
            width: 100%;
            padding: 8px;
            margin-top: 4px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }
        .btn {
            margin-top: 24px;
            padding: 12px 24px;
            background: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            box-shadow: 0 1px 4px rgba(0,0,0,0.08);
            transition: background 0.2s;
        }
        .btn:hover {
            background: #0056b3;
        }
        .register-link {
            display: block;
            margin-top: 24px;
            color: #007bff;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Doctor Login</h2>
        <form action="/doctors/login" method="POST">
            <label for="contact">Email</label>
            <input type="email" id="contact" name="contact" required>

            <label for="password">Password</label>
            <input type="password" id="password" name="password" required>

            <button type="submit" class="btn">Login</button>
        </form>
    <a href="/doctors/register" class="register-link">Don't have an account? Register here</a>
    </div>
</body>
</html>
