<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Patient Dashboard - MediSync</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: Arial, sans-serif;
            background: url('https://images.pexels.com/photos/4197564/pexels-photo-4197564.jpeg') no-repeat center center fixed;
            background-size: cover;
            min-height: 100vh;
        }
        .header {
            background: transparent;
            text-align: center;
            padding: 20px 0;
        }
        .header h1 {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-size: 48px;
            font-weight: 800;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }
        .navbar {
            background: rgba(255, 255, 255, 0.3);
            backdrop-filter: blur(10px);
            padding: 16px 40px;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .navbar .patient-name {
            color: #2c3e50;
            font-size: 18px;
            font-weight: 600;
        }
        .navbar .nav-links {
            display: flex;
            gap: 24px;
        }
        .navbar .nav-links a {
            color: #2c3e50;
            text-decoration: none;
            font-size: 16px;
            font-weight: 500;
            transition: opacity 0.2s;
        }
        .navbar .nav-links a:hover {
            opacity: 0.7;
            text-decoration: underline;
        }
        .container {
            max-width: 1000px;
            margin: 40px auto;
            background: rgba(255,255,255,0.95);
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 4px 16px rgba(0,0,0,0.1);
        }
        h2 {
            color: #4169E1;
            margin-bottom: 32px;
            text-align: center;
            font-size: 28px;
        }
        .spec-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
            gap: 20px;
            margin-top: 24px;
        }
        .spec-card {
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 1px 6px rgba(0,0,0,0.08);
            padding: 32px 24px;
            text-align: center;
            cursor: pointer;
            font-size: 18px;
            color: #007bff;
            font-weight: 500;
            transition: background 0.2s, color 0.2s, transform 0.2s;
            text-decoration: none;
            display: block;
        }
        .spec-card:hover {
            background: #e3f2fd;
            color: #0056b3;
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <!-- Hospital Header -->
    <div class="header">
        <h1>MediSync</h1>
    </div>

    <!-- Navigation Bar -->
    <div class="navbar">
        <div class="patient-name">Welcome, <%= patient.name %></div>
        <div class="nav-links">
            <a href="/patients/my-appointments">My Appointments</a>
            <a href="/patients/dashboard">Dashboard</a>
        </div>
    </div>

    <!-- Main Content -->
    <div class="container">
        <h2>Choose a Medical Specialization</h2>
        <div class="spec-list">
            <% specializations.forEach(function(spec) { %>
                <a href="/patients/dashboard/<%= encodeURIComponent(spec) %>" class="spec-card">
                    <%= spec %>
                </a>
            <% }); %>
        </div>
    </div>
</body>
</html>
