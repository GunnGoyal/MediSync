<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard – Medisync</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="/js/dashboardCache.js"></script>
    <style>
        :root {
            --primary: #0066ff;
            --primary-dark: #0052cc;
            --secondary: #00d4aa;
            --secondary-dark: #00a8cc;
            --accent: #ff6b6b;
            --warning: #fbbf24;
            --danger: #ef4444;
            --success: #10b981;
            --dark: #1a1a2e;
            --light: #f8fafc;
            --gradient-hero: linear-gradient(135deg, #0066ff 0%, #00d4aa 50%, #667eea 100%);
            --gradient-admin: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            --gradient-danger: linear-gradient(135deg, #ef4444 0%, #dc2626 50%, #b91c1c 100%);
            --gradient-success: linear-gradient(135deg, #10b981 0%, #059669 100%);
            --gradient-warning: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
            --gradient-info: linear-gradient(135deg, #3b82f6 0%, #6366f1 100%);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--light);
            color: var(--dark);
            min-height: 100vh;
        }

        /* Animated Background */
        .bg-animated {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            opacity: 0.05;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Navbar */
        .navbar-custom {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            box-shadow: 0 2px 30px rgba(0, 0, 0, 0.08);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar-brand {
            font-size: 1.5rem;
            font-weight: 800;
            background: var(--gradient-admin);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .admin-badge {
            background: var(--gradient-admin);
            color: white;
            padding: 0.35rem 1rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.85rem;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-logout {
            background: rgba(239, 68, 68, 0.1);
            color: var(--danger);
            border: none;
            padding: 0.5rem 1.25rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .btn-logout:hover {
            background: var(--danger);
            color: white;
            transform: translateY(-2px);
        }

        /* Page Header */
        .page-header {
            background: var(--gradient-admin);
            padding: 3rem 0;
            color: white;
            margin-bottom: 2rem;
            position: relative;
            overflow: hidden;
        }

        .page-header::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            transform: translate(50%, -50%);
        }

        .page-header::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
            transform: translate(-50%, 50%);
        }

        .page-header h1 {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            position: relative;
            z-index: 1;
        }

        .page-header p {
            opacity: 0.9;
            font-size: 1.1rem;
            position: relative;
            z-index: 1;
        }

        /* Content Container */
        .content-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 1.5rem 3rem;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: white;
            border-radius: 20px;
            padding: 1.75rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
            border: 1px solid rgba(0, 0, 0, 0.05);
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
        }

        .stat-card.primary::before { background: var(--gradient-info); }
        .stat-card.success::before { background: var(--gradient-success); }
        .stat-card.warning::before { background: var(--gradient-warning); }
        .stat-card.info::before { background: var(--gradient-admin); }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        }

        .stat-icon {
            width: 60px;
            height: 60px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        .stat-icon.primary {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(99, 102, 241, 0.1) 100%);
            color: #3b82f6;
        }

        .stat-icon.success {
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.1) 0%, rgba(5, 150, 105, 0.1) 100%);
            color: var(--success);
        }

        .stat-icon.warning {
            background: linear-gradient(135deg, rgba(251, 191, 36, 0.1) 0%, rgba(245, 158, 11, 0.1) 100%);
            color: #d97706;
        }

        .stat-icon.info {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
            color: #667eea;
        }

        .stat-card h6 {
            font-weight: 600;
            color: #64748b;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 800;
            background: var(--gradient-admin);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Menu Grid */
        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .menu-card {
            background: white;
            border-radius: 20px;
            padding: 1.75rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
            border: 1px solid rgba(0, 0, 0, 0.05);
            transition: all 0.4s ease;
            text-decoration: none;
            color: inherit;
            display: block;
            position: relative;
            overflow: hidden;
        }

        .menu-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient-admin);
            opacity: 0;
            transition: opacity 0.4s ease;
            z-index: 0;
        }

        .menu-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(102, 126, 234, 0.15);
            color: white;
        }

        .menu-card:hover::before {
            opacity: 1;
        }

        .menu-card:hover .menu-content {
            position: relative;
            z-index: 1;
        }

        .menu-card:hover .menu-icon {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }

        .menu-card:hover h5,
        .menu-card:hover p {
            color: white;
        }

        .menu-card:hover .menu-badge {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }

        .menu-content {
            position: relative;
            z-index: 1;
        }

        .menu-header {
            display: flex;
            align-items: flex-start;
            gap: 1rem;
            margin-bottom: 1rem;
        }

        .menu-icon {
            width: 50px;
            height: 50px;
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.25rem;
            flex-shrink: 0;
            transition: all 0.4s ease;
        }

        .menu-icon.purple {
            background: rgba(102, 126, 234, 0.1);
            color: #667eea;
        }

        .menu-icon.blue {
            background: rgba(59, 130, 246, 0.1);
            color: #3b82f6;
        }

        .menu-icon.green {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success);
        }

        .menu-icon.orange {
            background: rgba(245, 158, 11, 0.1);
            color: #d97706;
        }

        .menu-icon.red {
            background: rgba(239, 68, 68, 0.1);
            color: var(--danger);
        }

        .menu-icon.teal {
            background: rgba(0, 212, 170, 0.1);
            color: var(--secondary-dark);
        }

        .menu-card h5 {
            font-weight: 700;
            color: var(--dark);
            margin-bottom: 0.25rem;
            font-size: 1.1rem;
            transition: color 0.4s ease;
        }

        .menu-card p {
            color: #64748b;
            font-size: 0.9rem;
            margin: 0;
            transition: color 0.4s ease;
        }

        .menu-badges {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .menu-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.35rem;
            padding: 0.35rem 0.75rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.75rem;
            transition: all 0.4s ease;
        }

        .menu-badge.success {
            background: rgba(16, 185, 129, 0.1);
            color: var(--success);
        }

        .menu-badge.warning {
            background: rgba(245, 158, 11, 0.1);
            color: #d97706;
        }

        .menu-badge.primary {
            background: rgba(59, 130, 246, 0.1);
            color: #3b82f6;
        }

        .menu-badge.info {
            background: rgba(102, 126, 234, 0.1);
            color: #667eea;
        }

        /* Activity Card */
        .activity-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .activity-header {
            background: linear-gradient(135deg, #1a1a2e 0%, #2d2d44 100%);
            padding: 1.25rem 1.5rem;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .activity-header h5 {
            font-size: 1.1rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin: 0;
        }

        .btn-refresh {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.85rem;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-refresh:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
        }

        .activity-body {
            padding: 0;
        }

        .activity-table {
            width: 100%;
            border-collapse: collapse;
        }

        .activity-table thead th {
            background: #f8fafc;
            padding: 1rem 1.25rem;
            text-align: left;
            font-weight: 600;
            color: var(--dark);
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border-bottom: 2px solid #e2e8f0;
        }

        .activity-table tbody td {
            padding: 1rem 1.25rem;
            border-bottom: 1px solid #f1f5f9;
            vertical-align: middle;
        }

        .activity-table tbody tr {
            transition: all 0.3s ease;
        }

        .activity-table tbody tr:hover {
            background: rgba(102, 126, 234, 0.03);
        }

        .activity-table tbody tr:last-child td {
            border-bottom: none;
        }

        .timestamp {
            font-size: 0.85rem;
            color: #64748b;
        }

        .role-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.35rem;
            padding: 0.35rem 0.75rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.75rem;
            background: rgba(102, 126, 234, 0.1);
            color: #667eea;
        }

        .action-type-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.35rem;
            padding: 0.35rem 0.75rem;
            border-radius: 8px;
            font-weight: 600;
            font-size: 0.75rem;
            background: rgba(59, 130, 246, 0.1);
            color: #3b82f6;
        }

        .action-text {
            color: var(--dark);
            font-size: 0.9rem;
        }

        .empty-state {
            text-align: center;
            padding: 3rem 2rem;
            color: #64748b;
        }

        .empty-state i {
            font-size: 3rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        /* Footer */
        .page-footer {
            text-align: center;
            padding: 2rem;
            color: #94a3b8;
            font-size: 0.9rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .page-header h1 {
                font-size: 1.75rem;
            }

            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .menu-grid {
                grid-template-columns: 1fr;
            }

            .activity-table {
                display: block;
                overflow-x: auto;
            }
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .stat-card, .menu-card, .activity-card {
            animation: fadeInUp 0.6s ease forwards;
        }

        .stat-card:nth-child(1) { animation-delay: 0.1s; }
        .stat-card:nth-child(2) { animation-delay: 0.15s; }
        .stat-card:nth-child(3) { animation-delay: 0.2s; }
        .stat-card:nth-child(4) { animation-delay: 0.25s; }

        .menu-card:nth-child(1) { animation-delay: 0.3s; }
        .menu-card:nth-child(2) { animation-delay: 0.35s; }
        .menu-card:nth-child(3) { animation-delay: 0.4s; }
        .menu-card:nth-child(4) { animation-delay: 0.45s; }
        .menu-card:nth-child(5) { animation-delay: 0.5s; }
        .menu-card:nth-child(6) { animation-delay: 0.55s; }
        .menu-card:nth-child(7) { animation-delay: 0.6s; }
    </style>
</head>
<body>
    <div class="bg-animated"></div>

    <!-- Navbar -->
    <nav class="navbar-custom">
        <div class="container d-flex justify-content-between align-items-center">
            <a class="navbar-brand" href="/">🛡️ Medisync Admin</a>
            <div class="d-flex align-items-center gap-3">
                <span class="admin-badge">
                    <i class="fas fa-shield-alt"></i> Admin Panel
                </span>
                <form method="post" action="/admin/logout" class="d-inline m-0">
                    <button type="submit" class="btn-logout">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </button>
                </form>
            </div>
        </div>
    </nav>

    <!-- Page Header -->
    <div class="page-header">
        <div class="container">
            <h1>
                <i class="fas fa-chart-pie"></i>
                Admin Dashboard
            </h1>
            <p>System Overview & Control Panel</p>
        </div>
    </div>

    <!-- Main Content -->
    <div class="content-container">
        <!-- Statistics Cards -->
        <div class="stats-grid">
            <div class="stat-card primary">
                <div class="stat-icon primary">
                    <i class="fas fa-users"></i>
                </div>
                <h6>Total Patients</h6>
                <div class="stat-number"><%= stats.total_patients %></div>
            </div>
            <div class="stat-card success">
                <div class="stat-icon success">
                    <i class="fas fa-user-md"></i>
                </div>
                <h6>Total Doctors</h6>
                <div class="stat-number"><%= stats.total_doctors %></div>
            </div>
            <div class="stat-card warning">
                <div class="stat-icon warning">
                    <i class="fas fa-clock"></i>
                </div>
                <h6>Unverified Doctors</h6>
                <div class="stat-number"><%= stats.unverified_doctors %></div>
            </div>
            <div class="stat-card info">
                <div class="stat-icon info">
                    <i class="fas fa-calendar-check"></i>
                </div>
                <h6>Total Appointments</h6>
                <div class="stat-number"><%= stats.total_appointments %></div>
            </div>
        </div>

        <!-- Menu Grid -->
        <div class="menu-grid">
            <a href="/admin/doctors" class="menu-card">
                <div class="menu-content">
                    <div class="menu-header">
                        <div class="menu-icon purple">
                            <i class="fas fa-user-md"></i>
                        </div>
                        <div>
                            <h5>Doctor Management</h5>
                            <p>Verify, activate, deactivate doctors</p>
                        </div>
                    </div>
                    <div class="menu-badges">
                        <span class="menu-badge success"><i class="fas fa-check"></i> <%= stats.total_doctors %> registered</span>
                        <span class="menu-badge warning"><i class="fas fa-clock"></i> <%= stats.unverified_doctors %> pending</span>
                    </div>
                </div>
            </a>

            <a href="/admin/patients" class="menu-card">
                <div class="menu-content">
                    <div class="menu-header">
                        <div class="menu-icon blue">
                            <i class="fas fa-users"></i>
                        </div>
                        <div>
                            <h5>Patient Management</h5>
                            <p>Monitor and manage patient accounts</p>
                        </div>
                    </div>
                    <div class="menu-badges">
                        <span class="menu-badge primary"><i class="fas fa-user"></i> <%= stats.total_patients %> registered</span>
                    </div>
                </div>
            </a>

            <a href="/admin/appointments" class="menu-card">
                <div class="menu-content">
                    <div class="menu-header">
                        <div class="menu-icon green">
                            <i class="fas fa-calendar-alt"></i>
                        </div>
                        <div>
                            <h5>Appointment Monitoring</h5>
                            <p>View all appointments and status</p>
                        </div>
                    </div>
                    <div class="menu-badges">
                        <span class="menu-badge info"><i class="fas fa-calendar"></i> <%= stats.total_appointments %> total</span>
                    </div>
                </div>
            </a>

            <a href="/admin/analytics" class="menu-card">
                <div class="menu-content">
                    <div class="menu-header">
                        <div class="menu-icon teal">
                            <i class="fas fa-chart-bar"></i>
                        </div>
                        <div>
                            <h5>Analytics</h5>
                            <p>Medicine, disease, and prescription trends</p>
                        </div>
                    </div>
                    <div class="menu-badges">
                        <span class="menu-badge success"><i class="fas fa-prescription"></i> <%= stats.total_prescriptions %> prescriptions</span>
                    </div>
                </div>
            </a>

            <a href="/admin/predictions" class="menu-card">
                <div class="menu-content">
                    <div class="menu-header">
                        <div class="menu-icon orange">
                            <i class="fas fa-brain"></i>
                        </div>
                        <div>
                            <h5>Prediction Oversight</h5>
                            <p>Monitor medicine repetition alerts</p>
                        </div>
                    </div>
                </div>
            </a>

            <a href="/admin/audit-logs" class="menu-card">
                <div class="menu-content">
                    <div class="menu-header">
                        <div class="menu-icon red">
                            <i class="fas fa-scroll"></i>
                        </div>
                        <div>
                            <h5>Audit Logs</h5>
                            <p>Track all system activities</p>
                        </div>
                    </div>
                </div>
            </a>

            <a href="/admin/config" class="menu-card">
                <div class="menu-content">
                    <div class="menu-header">
                        <div class="menu-icon purple">
                            <i class="fas fa-cog"></i>
                        </div>
                        <div>
                            <h5>System Configuration</h5>
                            <p>Maintenance mode, notices, backups</p>
                        </div>
                    </div>
                </div>
            </a>
        </div>

        <!-- Recent Activity -->
        <div class="activity-card">
            <div class="activity-header">
                <h5><i class="fas fa-history"></i> Recent Activity</h5>
                <button class="btn-refresh" onclick="location.reload()">
                    <i class="fas fa-sync-alt"></i> Refresh
                </button>
            </div>
            <div class="activity-body">
                <% if (recent && recent.length > 0) { %>
                    <table class="activity-table">
                        <thead>
                            <tr>
                                <th>Timestamp</th>
                                <th>User Role</th>
                                <th>Action</th>
                                <th>Type</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% recent.forEach(log => { %>
                                <tr>
                                    <td>
                                        <span class="timestamp">
                                            <i class="fas fa-clock"></i> <%= new Date(log.timestamp).toLocaleString() %>
                                        </span>
                                    </td>
                                    <td>
                                        <span class="role-badge"><%= log.user_role %></span>
                                    </td>
                                    <td>
                                        <span class="action-text"><%= log.action %></span>
                                    </td>
                                    <td>
                                        <span class="action-type-badge"><%= log.action_type %></span>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                <% } else { %>
                    <div class="empty-state">
                        <i class="fas fa-inbox"></i>
                        <p>No recent activity</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <div class="page-footer">
        <p>© 2025 Medisync Admin Panel. Connecting Care, Predicting Health.</p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Initialize dashboard cache on page load
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize cache UI with controls
            if (typeof DashboardCache !== 'undefined') {
                DashboardCache.initUI();
                console.log('[Admin Dashboard] Cache initialized');
                console.log('[Admin Dashboard] Cache Stats:', DashboardCache.getStats());
                
                DashboardCache.set('admin_dashboard_stats', {
                    timestamp: new Date().toISOString(),
                    page: 'admin_dashboard'
                }, DashboardCache.CONFIG.ADMIN_TTL);
            }
        });
    </script>
</body>
</html>
