<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Disease Patterns - Medisync</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.7.0/dist/chart.min.js"></script>
    <style>
        :root {
            --primary: #0066ff;
            --primary-dark: #0052cc;
            --secondary: #00d4aa;
            --secondary-dark: #00a8cc;
            --accent: #ff6b6b;
            --warning: #fbbf24;
            --danger: #ef4444;
            --success: #10b981;
            --dark: #1a1a2e;
            --light: #f8fafc;
            --gradient-hero: linear-gradient(135deg, #0066ff 0%, #00d4aa 50%, #667eea 100%);
            --gradient-patient: linear-gradient(135deg, #0066ff 0%, #0052cc 50%, #667eea 100%);
            --gradient-disease: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            --gradient-danger: linear-gradient(135deg, #ef4444 0%, #dc2626 50%, #b91c1c 100%);
            --gradient-warning: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
            --gradient-success: linear-gradient(135deg, #10b981 0%, #059669 100%);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--light);
            color: var(--dark);
            min-height: 100vh;
        }

        /* Animated Background */
        .bg-animated {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            opacity: 0.05;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Navbar */
        .navbar-custom {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            box-shadow: 0 2px 30px rgba(0, 0, 0, 0.08);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .navbar-brand {
            font-size: 1.5rem;
            font-weight: 800;
            background: var(--gradient-hero);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .btn-back {
            background: rgba(0, 102, 255, 0.1);
            color: var(--primary);
            border: none;
            padding: 0.5rem 1.25rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-back:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-2px);
        }

        /* Page Header */
        .page-header {
            background: var(--gradient-disease);
            padding: 3rem 0;
            color: white;
            margin-bottom: 2rem;
            position: relative;
            overflow: hidden;
        }

        .page-header::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            transform: translate(50%, -50%);
        }

        .page-header::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
            transform: translate(-50%, 50%);
        }

        .page-header h1 {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            position: relative;
            z-index: 1;
        }

        .page-header p {
            opacity: 0.9;
            font-size: 1.1rem;
            position: relative;
            z-index: 1;
        }

        /* Content Container */
        .content-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1.5rem 3rem;
        }

        /* Success State */
        .success-state {
            text-align: center;
            padding: 3rem 2rem;
            background: white;
            border-radius: 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
            margin-bottom: 2rem;
        }

        .success-icon {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            background: var(--gradient-success);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            font-size: 2.5rem;
            color: white;
            animation: pulse-success 2s ease-in-out infinite;
        }

        @keyframes pulse-success {
            0%, 100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.4); }
            50% { transform: scale(1.05); box-shadow: 0 0 0 15px rgba(16, 185, 129, 0); }
        }

        .success-state h3 {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--success);
            margin-bottom: 0.5rem;
        }

        .success-state p {
            color: #64748b;
            font-size: 1rem;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: white;
            border-radius: 20px;
            padding: 1.75rem;
            text-align: center;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
            border: 1px solid rgba(0, 0, 0, 0.05);
            transition: all 0.4s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        }

        .stat-icon {
            width: 60px;
            height: 60px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin: 0 auto 1rem;
        }

        .stat-icon.primary {
            background: linear-gradient(135deg, rgba(0, 102, 255, 0.1) 0%, rgba(102, 126, 234, 0.1) 100%);
            color: var(--primary);
        }

        .stat-icon.danger {
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.1) 0%, rgba(220, 38, 38, 0.1) 100%);
            color: var(--danger);
        }

        .stat-icon.warning {
            background: linear-gradient(135deg, rgba(251, 191, 36, 0.1) 0%, rgba(245, 158, 11, 0.1) 100%);
            color: #d97706;
        }

        .stat-icon.success {
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.1) 0%, rgba(5, 150, 105, 0.1) 100%);
            color: var(--success);
        }

        .stat-card h5 {
            font-weight: 600;
            color: #64748b;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: 800;
            background: var(--gradient-disease);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Disease Table Card */
        .table-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
            border: 1px solid rgba(0, 0, 0, 0.05);
            margin-bottom: 2rem;
        }

        .table-card-header {
            background: var(--gradient-disease);
            padding: 1.25rem 1.5rem;
            color: white;
        }

        .table-card-header h5 {
            font-size: 1.1rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin: 0;
        }

        .disease-table {
            width: 100%;
            border-collapse: collapse;
        }

        .disease-table thead th {
            background: #f8fafc;
            padding: 1rem 1.25rem;
            text-align: left;
            font-weight: 600;
            color: var(--dark);
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            border-bottom: 2px solid #e2e8f0;
        }

        .disease-table tbody td {
            padding: 1.25rem;
            border-bottom: 1px solid #f1f5f9;
            vertical-align: middle;
        }

        .disease-table tbody tr {
            transition: all 0.3s ease;
        }

        .disease-table tbody tr:hover {
            background: rgba(102, 126, 234, 0.03);
        }

        .disease-name {
            font-weight: 700;
            color: var(--dark);
        }

        .frequency-badge {
            background: rgba(0, 212, 170, 0.1);
            color: var(--secondary-dark);
            padding: 0.35rem 0.75rem;
            border-radius: 8px;
            font-weight: 600;
            font-size: 0.85rem;
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.35rem;
            padding: 0.35rem 0.75rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.8rem;
        }

        .status-badge.chronic {
            background: rgba(239, 68, 68, 0.1);
            color: var(--danger);
        }

        .status-badge.recurring {
            background: rgba(251, 191, 36, 0.1);
            color: #d97706;
        }

        .risk-text {
            color: #64748b;
            font-size: 0.9rem;
        }

        /* Analysis Cards */
        .analysis-section {
            margin-top: 2rem;
        }

        .analysis-section-title {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--dark);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .analysis-section-title i {
            color: #667eea;
        }

        .analysis-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
            border: 1px solid rgba(0, 0, 0, 0.05);
            margin-bottom: 1.5rem;
            transition: all 0.4s ease;
        }

        .analysis-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.1);
        }

        .analysis-card-header {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            padding: 1.25rem 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #e2e8f0;
        }

        .analysis-card-header h6 {
            font-weight: 700;
            color: var(--dark);
            margin: 0;
            font-size: 1rem;
        }

        .analysis-card-body {
            padding: 1.5rem;
        }

        .analysis-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .analysis-item {
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
        }

        .analysis-label {
            font-size: 0.8rem;
            color: #94a3b8;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .analysis-value {
            font-weight: 600;
            color: var(--dark);
        }

        .analysis-value .badge {
            margin-left: 0.5rem;
        }

        .risk-assessment-box {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);
            border-radius: 12px;
            padding: 1.25rem;
            border: 1px solid rgba(102, 126, 234, 0.1);
        }

        .risk-assessment-box h6 {
            font-weight: 700;
            color: #667eea;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.9rem;
        }

        .risk-assessment-box p {
            color: #64748b;
            margin: 0;
            font-size: 0.95rem;
        }

        .chronic-indicator {
            color: var(--danger);
            font-size: 0.85rem;
        }

        /* Recommendations Card */
        .recommendations-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
            border: 1px solid rgba(0, 0, 0, 0.05);
            margin-top: 2rem;
            border-left: 4px solid var(--warning);
        }

        .recommendations-header {
            background: linear-gradient(135deg, rgba(251, 191, 36, 0.1) 0%, rgba(245, 158, 11, 0.1) 100%);
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid rgba(251, 191, 36, 0.2);
        }

        .recommendations-header h5 {
            font-size: 1.1rem;
            font-weight: 700;
            color: #d97706;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin: 0;
        }

        .recommendations-body {
            padding: 1.5rem;
        }

        .recommendation-item {
            background: rgba(0, 102, 255, 0.05);
            border-radius: 12px;
            padding: 1rem 1.25rem;
            margin-bottom: 0.75rem;
            border-left: 3px solid var(--primary);
        }

        .recommendation-item:last-child {
            margin-bottom: 0;
        }

        .recommendation-type {
            font-weight: 700;
            color: var(--primary);
        }

        .recommendation-message {
            color: #64748b;
        }

        /* Back Buttons */
        .back-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 2rem;
        }

        .btn-back-main {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: white;
            color: var(--dark);
            border: 2px solid #e2e8f0;
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.95rem;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .btn-back-main:hover {
            border-color: var(--primary);
            color: var(--primary);
            transform: translateY(-2px);
        }

        .btn-back-main.primary {
            background: var(--gradient-hero);
            color: white;
            border: none;
        }

        .btn-back-main.primary:hover {
            box-shadow: 0 10px 25px rgba(0, 102, 255, 0.3);
            color: white;
        }

        /* Footer */
        .page-footer {
            text-align: center;
            padding: 2rem;
            color: #94a3b8;
            font-size: 0.9rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .page-header h1 {
                font-size: 1.75rem;
            }

            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .analysis-grid {
                grid-template-columns: 1fr;
            }

            .disease-table {
                display: block;
                overflow-x: auto;
            }
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .stat-card, .table-card, .analysis-card, .recommendations-card, .success-state {
            animation: fadeInUp 0.6s ease forwards;
        }

        .stat-card:nth-child(1) { animation-delay: 0.1s; }
        .stat-card:nth-child(2) { animation-delay: 0.15s; }
        .stat-card:nth-child(3) { animation-delay: 0.2s; }
        .stat-card:nth-child(4) { animation-delay: 0.25s; }
    </style>
</head>
<body>
    <div class="bg-animated"></div>

    <!-- Navbar -->
    <nav class="navbar-custom">
        <div class="container d-flex justify-content-between align-items-center">
            <a class="navbar-brand" href="/">🏥 Medisync</a>
            <a href="/health-intelligence/dashboard" class="btn-back">
                <i class="fas fa-arrow-left"></i> Back to Health Intelligence
            </a>
        </div>
    </nav>

    <!-- Page Header -->
    <div class="page-header">
        <div class="container">
            <h1>
                <i class="fas fa-chart-line"></i>
                Disease Pattern Analysis
            </h1>
            <p>Track recurring and chronic conditions detected in your health history</p>
        </div>
    </div>

    <!-- Main Content -->
    <div class="content-container">
        <% if (report.diseasePatterns.length === 0) { %>
            <div class="success-state">
                <div class="success-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <h3>Great News!</h3>
                <p>No recurring disease patterns detected in the last 6 months.</p>
            </div>
        <% } else { %>
            <!-- Summary Stats -->
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon primary">
                        <i class="fas fa-stethoscope"></i>
                    </div>
                    <h5>Total Patterns</h5>
                    <div class="stat-number"><%= report.diseasePatterns.length %></div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon danger">
                        <i class="fas fa-flask-vial"></i>
                    </div>
                    <h5>Chronic</h5>
                    <div class="stat-number"><%= report.diseasePatterns.filter(d => d.is_chronic).length %></div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon warning">
                        <i class="fas fa-sync"></i>
                    </div>
                    <h5>Recurring</h5>
                    <div class="stat-number"><%= report.diseasePatterns.filter(d => !d.is_chronic).length %></div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon success">
                        <i class="fas fa-calendar-days"></i>
                    </div>
                    <h5>Time Window</h5>
                    <div class="stat-number" style="font-size: 1.5rem;">6 Months</div>
                </div>
            </div>

            <!-- Detailed Disease List -->
            <div class="table-card">
                <div class="table-card-header">
                    <h5><i class="fas fa-list"></i> Detected Disease Patterns</h5>
                </div>
                <div class="table-responsive">
                    <table class="disease-table">
                        <thead>
                            <tr>
                                <th>Disease Name</th>
                                <th>Frequency</th>
                                <th>Status</th>
                                <th>Risk Assessment</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% report.diseasePatterns.forEach(pattern => { %>
                                <tr>
                                    <td>
                                        <span class="disease-name"><%= pattern.disease_name %></span>
                                    </td>
                                    <td>
                                        <span class="frequency-badge"><%= pattern.frequency %> occurrences</span>
                                    </td>
                                    <td>
                                        <% if (pattern.is_chronic) { %>
                                            <span class="status-badge chronic">
                                                <i class="fas fa-exclamation-circle"></i> Chronic
                                            </span>
                                        <% } else { %>
                                            <span class="status-badge recurring">
                                                <i class="fas fa-sync"></i> Recurring
                                            </span>
                                        <% } %>
                                    </td>
                                    <td>
                                        <span class="risk-text"><%= pattern.risk_assessment %></span>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Detailed Analysis -->
            <div class="analysis-section">
                <h5 class="analysis-section-title">
                    <i class="fas fa-microscope"></i> Detailed Analysis
                </h5>
                <% report.diseasePatterns.forEach((pattern, index) => { %>
                    <div class="analysis-card">
                        <div class="analysis-card-header">
                            <h6><%= pattern.disease_name %></h6>
                            <% if (pattern.is_chronic) { %>
                                <span class="status-badge chronic">Chronic Condition</span>
                            <% } %>
                        </div>
                        <div class="analysis-card-body">
                            <div class="analysis-grid">
                                <div class="analysis-item">
                                    <span class="analysis-label">Occurrence Frequency</span>
                                    <span class="analysis-value">
                                        <span class="frequency-badge"><%= pattern.frequency %> times in 6 months</span>
                                    </span>
                                </div>
                                <div class="analysis-item">
                                    <span class="analysis-label">Average Frequency</span>
                                    <span class="analysis-value"><%= (pattern.frequency / 6).toFixed(1) %> times per month</span>
                                </div>
                                <div class="analysis-item">
                                    <span class="analysis-label">Analysis Period</span>
                                    <span class="analysis-value">Last 6 months</span>
                                </div>
                                <div class="analysis-item">
                                    <span class="analysis-label">Threshold Status</span>
                                    <span class="analysis-value">
                                        <% if (pattern.frequency >= 3) { %>
                                            <span class="chronic-indicator">
                                                <i class="fas fa-check-circle"></i> Meets chronic criteria (≥3 occurrences)
                                            </span>
                                        <% } else { %>
                                            <span style="color: var(--success);">
                                                <i class="fas fa-info-circle"></i> Below chronic threshold
                                            </span>
                                        <% } %>
                                    </span>
                                </div>
                            </div>
                            <div class="risk-assessment-box">
                                <h6><i class="fas fa-lightbulb"></i> Assessment</h6>
                                <p><%= pattern.risk_assessment %></p>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>

            <!-- Recommendations -->
            <% if (report.recommendations && report.recommendations.length > 0) { %>
                <div class="recommendations-card">
                    <div class="recommendations-header">
                        <h5><i class="fas fa-clipboard-check"></i> Recommendations</h5>
                    </div>
                    <div class="recommendations-body">
                        <% report.recommendations.forEach(rec => { %>
                            <div class="recommendation-item">
                                <span class="recommendation-type"><%= rec.type %>:</span>
                                <span class="recommendation-message"><%= rec.message %></span>
                            </div>
                        <% }); %>
                    </div>
                </div>
            <% } %>
        <% } %>

        <!-- Back Buttons -->
        <div class="back-buttons">
            <a href="/health-intelligence/dashboard" class="btn-back-main primary">
                <i class="fas fa-brain"></i> Health Intelligence
            </a>
            <a href="/patient/dashboard" class="btn-back-main">
                <i class="fas fa-arrow-left"></i> Patient Dashboard
            </a>
        </div>
    </div>

    <!-- Footer -->
    <div class="page-footer">
        <p>© 2025 Medisync. Connecting Care, Predicting Health.</p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
