<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Health Prediction – Medisync</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --primary: #0066ff;
      --primary-dark: #0052cc;
      --secondary: #00d4aa;
      --secondary-dark: #00a8cc;
      --accent: #ff6b6b;
      --warning: #fbbf24;
      --danger: #ef4444;
      --success: #10b981;
      --dark: #1a1a2e;
      --light: #f8fafc;
      --gradient-hero: linear-gradient(135deg, #0066ff 0%, #00d4aa 50%, #667eea 100%);
      --gradient-patient: linear-gradient(135deg, #0066ff 0%, #0052cc 50%, #667eea 100%);
      --gradient-danger: linear-gradient(135deg, #ef4444 0%, #dc2626 50%, #b91c1c 100%);
      --gradient-warning: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
      --gradient-success: linear-gradient(135deg, #10b981 0%, #059669 100%);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--light);
      color: var(--dark);
      min-height: 100vh;
    }

    /* Animated Background */
    .bg-animated {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1;
      background: linear-gradient(-45deg, #0066ff, #00d4aa, #667eea, #764ba2);
      background-size: 400% 400%;
      animation: gradientBG 15s ease infinite;
      opacity: 0.03;
    }

    @keyframes gradientBG {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    /* Navbar */
    .navbar-custom {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      box-shadow: 0 2px 30px rgba(0, 0, 0, 0.08);
      padding: 1rem 0;
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    .navbar-brand {
      font-size: 1.5rem;
      font-weight: 800;
      background: var(--gradient-patient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      text-decoration: none;
    }

    .back-btn {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      color: var(--dark);
      text-decoration: none;
      font-weight: 600;
      padding: 0.5rem 1.25rem;
      border-radius: 50px;
      transition: all 0.3s ease;
      border: 2px solid #e2e8f0;
    }

    .back-btn:hover {
      border-color: var(--primary);
      color: var(--primary);
      background: rgba(0, 102, 255, 0.05);
    }

    /* Page Header */
    .page-header {
      background: var(--gradient-patient);
      padding: 3rem 0;
      margin-bottom: 2rem;
      position: relative;
      overflow: hidden;
    }

    .page-header::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -10%;
      width: 400px;
      height: 400px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
    }

    .page-header::after {
      content: '';
      position: absolute;
      bottom: -30%;
      left: -5%;
      width: 300px;
      height: 300px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.08);
    }

    .header-content {
      position: relative;
      z-index: 1;
      color: white;
    }

    .header-content h1 {
      font-size: 2rem;
      font-weight: 800;
      margin-bottom: 0.5rem;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .header-content p {
      opacity: 0.9;
      font-size: 1.1rem;
    }

    .header-stats {
      display: flex;
      gap: 1.5rem;
      margin-top: 1.5rem;
    }

    .header-stat {
      background: rgba(255, 255, 255, 0.15);
      padding: 1rem 1.5rem;
      border-radius: 16px;
      text-align: center;
    }

    .header-stat-number {
      font-size: 1.75rem;
      font-weight: 800;
    }

    .header-stat-label {
      font-size: 0.85rem;
      opacity: 0.9;
    }

    /* Medicine Cards Grid */
    .prediction-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2rem;
    }

    .medicine-card {
      background: white;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.06);
      border: 1px solid rgba(0, 0, 0, 0.04);
      transition: all 0.3s ease;
      position: relative;
    }

    .medicine-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.1);
    }

    .medicine-card-header {
      padding: 1.5rem;
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .medicine-icon {
      width: 60px;
      height: 60px;
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      flex-shrink: 0;
    }

    .medicine-icon.normal {
      background: rgba(0, 102, 255, 0.1);
      color: var(--primary);
    }

    .medicine-icon.warning {
      background: rgba(251, 191, 36, 0.15);
      color: #d97706;
    }

    .medicine-icon.danger {
      background: rgba(239, 68, 68, 0.15);
      color: var(--danger);
    }

    .medicine-info h4 {
      font-size: 1.15rem;
      font-weight: 700;
      color: var(--dark);
      margin-bottom: 0.25rem;
    }

    .usage-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.375rem 0.875rem;
      border-radius: 50px;
      font-size: 0.85rem;
      font-weight: 600;
    }

    .usage-badge.normal {
      background: rgba(0, 102, 255, 0.1);
      color: var(--primary);
    }

    .usage-badge.warning {
      background: rgba(251, 191, 36, 0.15);
      color: #d97706;
    }

    .usage-badge.danger {
      background: rgba(239, 68, 68, 0.15);
      color: var(--danger);
    }

    .medicine-card-body {
      padding: 0 1.5rem 1.5rem;
    }

    /* Risk Alert */
    .risk-alert {
      padding: 1rem 1.25rem;
      border-radius: 12px;
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
    }

    .risk-alert.danger {
      background: linear-gradient(135deg, rgba(239, 68, 68, 0.1) 0%, rgba(220, 38, 38, 0.05) 100%);
      border: 1px solid rgba(239, 68, 68, 0.2);
    }

    .risk-alert.warning {
      background: linear-gradient(135deg, rgba(251, 191, 36, 0.1) 0%, rgba(217, 119, 6, 0.05) 100%);
      border: 1px solid rgba(251, 191, 36, 0.2);
    }

    .risk-alert.info {
      background: linear-gradient(135deg, rgba(0, 102, 255, 0.1) 0%, rgba(0, 212, 170, 0.05) 100%);
      border: 1px solid rgba(0, 102, 255, 0.2);
    }

    .risk-alert-icon {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      font-size: 1rem;
    }

    .risk-alert-icon.danger {
      background: var(--gradient-danger);
      color: white;
    }

    .risk-alert-icon.warning {
      background: var(--gradient-warning);
      color: white;
    }

    .risk-alert-icon.info {
      background: var(--gradient-patient);
      color: white;
    }

    .risk-alert-content h5 {
      font-size: 0.9rem;
      font-weight: 700;
      margin-bottom: 0.25rem;
    }

    .risk-alert-content h5.danger { color: var(--danger); }
    .risk-alert-content h5.warning { color: #d97706; }
    .risk-alert-content h5.info { color: var(--primary); }

    .risk-alert-content p {
      font-size: 0.85rem;
      color: #64748b;
      margin: 0;
      line-height: 1.5;
    }

    /* Progress Bar */
    .usage-progress {
      margin-top: 1rem;
    }

    .usage-progress-label {
      display: flex;
      justify-content: space-between;
      font-size: 0.8rem;
      margin-bottom: 0.5rem;
    }

    .usage-progress-label span {
      color: #64748b;
    }

    .usage-progress-label strong {
      color: var(--dark);
    }

    .progress-bar-container {
      height: 8px;
      background: #e2e8f0;
      border-radius: 4px;
      overflow: hidden;
    }

    .progress-bar-fill {
      height: 100%;
      border-radius: 4px;
      transition: width 0.5s ease;
    }

    .progress-bar-fill.normal { background: var(--gradient-patient); }
    .progress-bar-fill.warning { background: var(--gradient-warning); }
    .progress-bar-fill.danger { background: var(--gradient-danger); }

    /* Healthy State */
    .healthy-state {
      background: white;
      border-radius: 24px;
      padding: 4rem 2rem;
      text-align: center;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.06);
      border: 1px solid rgba(0, 0, 0, 0.04);
    }

    .healthy-icon {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      background: var(--gradient-success);
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.5rem;
      font-size: 3rem;
      color: white;
      box-shadow: 0 15px 40px rgba(16, 185, 129, 0.3);
    }

    .healthy-state h3 {
      font-size: 1.75rem;
      font-weight: 800;
      color: var(--dark);
      margin-bottom: 0.75rem;
    }

    .healthy-state p {
      color: #64748b;
      font-size: 1.1rem;
      max-width: 500px;
      margin: 0 auto 2rem;
    }

    .healthy-tips {
      display: flex;
      justify-content: center;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .healthy-tip {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      background: rgba(16, 185, 129, 0.1);
      padding: 0.5rem 1rem;
      border-radius: 50px;
      font-size: 0.9rem;
      color: #059669;
      font-weight: 500;
    }

    /* How It Works Card */
    .info-card {
      background: white;
      border-radius: 24px;
      padding: 2rem;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.06);
      border: 1px solid rgba(0, 0, 0, 0.04);
      margin-bottom: 2rem;
    }

    .info-card-header {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1.5rem;
    }

    .info-card-icon {
      width: 50px;
      height: 50px;
      border-radius: 14px;
      background: var(--gradient-patient);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.25rem;
    }

    .info-card-header h4 {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--dark);
      margin: 0;
    }

    .risk-levels {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1rem;
    }

    .risk-level-item {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 1rem;
      background: #f8fafc;
      border-radius: 12px;
      transition: all 0.3s ease;
    }

    .risk-level-item:hover {
      background: #f1f5f9;
    }

    .risk-level-indicator {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      flex-shrink: 0;
    }

    .risk-level-indicator.normal { background: var(--primary); }
    .risk-level-indicator.warning { background: #f59e0b; }
    .risk-level-indicator.danger { background: var(--danger); }

    .risk-level-content h5 {
      font-size: 0.9rem;
      font-weight: 700;
      color: var(--dark);
      margin-bottom: 0.25rem;
    }

    .risk-level-content p {
      font-size: 0.85rem;
      color: #64748b;
      margin: 0;
    }

    /* Footer Actions */
    .page-footer {
      text-align: center;
      padding: 2rem 0 3rem;
    }

    .btn-back-dashboard {
      background: white;
      color: var(--primary);
      border: 2px solid var(--primary);
      padding: 0.875rem 2rem;
      border-radius: 50px;
      font-weight: 600;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      transition: all 0.3s ease;
    }

    .btn-back-dashboard:hover {
      background: var(--gradient-patient);
      color: white;
      border-color: transparent;
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(0, 102, 255, 0.2);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .header-content h1 {
        font-size: 1.5rem;
      }

      .header-stats {
        flex-direction: column;
        gap: 1rem;
      }

      .prediction-grid {
        grid-template-columns: 1fr;
      }

      .risk-levels {
        grid-template-columns: 1fr;
      }

      .healthy-tips {
        flex-direction: column;
        align-items: center;
      }
    }

    /* Animations */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in {
      animation: fadeInUp 0.5s ease forwards;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }

    .pulse-animation {
      animation: pulse 2s ease-in-out infinite;
    }
  </style>
</head>
<body>
  <div class="bg-animated"></div>

  <!-- Navbar -->
  <nav class="navbar-custom">
    <div class="container">
      <div class="d-flex align-items-center justify-content-between">
        <a class="navbar-brand" href="/">
          <span>🏥</span> Medisync
        </a>
        <a href="/patient/dashboard" class="back-btn">
          <i class="fas fa-arrow-left"></i>
          <span>Back to Dashboard</span>
        </a>
      </div>
    </div>
  </nav>

  <!-- Page Header -->
  <section class="page-header">
    <div class="container">
      <div class="header-content">
        <div class="row align-items-center">
          <div class="col-md-8">
            <h1>
              <i class="fas fa-brain"></i>
              Health Predictions
            </h1>
            <p>AI-powered analysis of your medicine usage patterns and health risks</p>
          </div>
          <div class="col-md-4">
            <div class="header-stats">
              <div class="header-stat">
                <div class="header-stat-number"><%= stats ? stats.length : 0 %></div>
                <div class="header-stat-label">Medicines Analyzed</div>
              </div>
              <div class="header-stat">
                <div class="header-stat-number">2</div>
                <div class="header-stat-label">Months Data</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="container">
    <% if (stats && stats.length > 0) { %>
      <!-- Medicine Analysis Cards -->
      <div class="prediction-grid">
        <% stats.forEach((s, index) => { %>
          <% 
            let riskLevel = 'normal';
            let riskIcon = 'fa-info-circle';
            let riskTitle = 'Regular Usage';
            let riskMessage = 'This medicine is being used within normal frequency. Continue as prescribed.';
            let progressWidth = Math.min((s.count / 8) * 100, 100);
            
            if (s.count >= 6) {
              riskLevel = 'danger';
              riskIcon = 'fa-exclamation-triangle';
              riskTitle = 'High Risk Alert';
              riskMessage = 'Possible chronic condition detected. Please consult your doctor for evaluation.';
            } else if (s.count >= 4) {
              riskLevel = 'warning';
              riskIcon = 'fa-exclamation-circle';
              riskTitle = 'Dependency Warning';
              riskMessage = 'Medicine used frequently. Consider monitoring your usage pattern.';
            }
          %>
          <div class="medicine-card animate-fade-in" style="animation-delay: <%= index * 0.1 %>s">
            <div class="medicine-card-header">
              <div class="medicine-icon <%= riskLevel %>">
                <i class="fas fa-pills"></i>
              </div>
              <div class="medicine-info">
                <h4><%= s.medicine_name %></h4>
                <span class="usage-badge <%= riskLevel %>">
                  <i class="fas fa-chart-bar"></i>
                  Used <%= s.count %> times
                </span>
              </div>
            </div>
            <div class="medicine-card-body">
              <div class="risk-alert <%= riskLevel %>">
                <div class="risk-alert-icon <%= riskLevel %>">
                  <i class="fas <%= riskIcon %>"></i>
                </div>
                <div class="risk-alert-content">
                  <h5 class="<%= riskLevel %>"><%= riskTitle %></h5>
                  <p><%= riskMessage %></p>
                </div>
              </div>
              <div class="usage-progress">
                <div class="usage-progress-label">
                  <span>Usage Level</span>
                  <strong><%= s.count %> / 8+ threshold</strong>
                </div>
                <div class="progress-bar-container">
                  <div class="progress-bar-fill <%= riskLevel %>" style="width: <%= progressWidth %>%"></div>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <!-- Healthy State -->
      <div class="healthy-state animate-fade-in">
        <div class="healthy-icon pulse-animation">
          <i class="fas fa-heart"></i>
        </div>
        <h3>You're in Great Shape! 🎉</h3>
        <p>No medicine repetition patterns detected in the last 2 months. Keep up your healthy habits and lifestyle!</p>
        <div class="healthy-tips">
          <div class="healthy-tip">
            <i class="fas fa-check-circle"></i>
            No dependencies detected
          </div>
          <div class="healthy-tip">
            <i class="fas fa-check-circle"></i>
            Normal medication pattern
          </div>
          <div class="healthy-tip">
            <i class="fas fa-check-circle"></i>
            Low health risk
          </div>
        </div>
      </div>
    <% } %>

    <!-- How It Works -->
    <div class="info-card">
      <div class="info-card-header">
        <div class="info-card-icon">
          <i class="fas fa-chart-pie"></i>
        </div>
        <h4>How AI Prediction Works</h4>
      </div>
      <p style="color: #64748b; margin-bottom: 1.5rem;">
        Our intelligent system analyzes your medicine history to detect repetition patterns that may indicate health concerns:
      </p>
      <div class="risk-levels">
        <div class="risk-level-item">
          <div class="risk-level-indicator normal"></div>
          <div class="risk-level-content">
            <h5>1-3 Times Usage</h5>
            <p>Normal usage pattern. No concerns detected.</p>
          </div>
        </div>
        <div class="risk-level-item">
          <div class="risk-level-indicator warning"></div>
          <div class="risk-level-content">
            <h5>4-5 Times Usage</h5>
            <p>Possible dependency. Monitor and consult if concerned.</p>
          </div>
        </div>
        <div class="risk-level-item">
          <div class="risk-level-indicator danger"></div>
          <div class="risk-level-content">
            <h5>6+ Times Usage</h5>
            <p>High risk indicator. Please consult your doctor.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div class="page-footer">
      <a href="/patient/dashboard" class="btn-back-dashboard">
        <i class="fas fa-home"></i>
        Return to Dashboard
      </a>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>